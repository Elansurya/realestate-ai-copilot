# ---------------------------------------------------------------------
# Enterprise Real Estate AI Copilot CRM - Environment Variables
# Copy this file to `.env` and fill in real values.
# NEVER commit the actual `.env` file to version control.
# ---------------------------------------------------------------------

# --- General ---
PROJECT_NAME="Enterprise Real Estate AI Copilot CRM"
ENVIRONMENT=development
DEBUG=true

# --- Server ---
HOST=0.0.0.0
PORT=8000

# --- Security / JWT ---
SECRET_KEY=change_this_to_a_long_random_secret_value
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- PostgreSQL Database ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_SERVER=db
POSTGRES_PORT=5432
POSTGRES_DB=real_estate_crm

# Optional: overrides the auto-built connection string above if set.
# DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/real_estate_crm

DB_ECHO=false
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800

# --- CORS (comma-separated list of allowed origins) ---
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# --- Logging ---
LOG_LEVEL=INFO